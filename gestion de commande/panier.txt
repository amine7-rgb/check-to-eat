/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package service;

import entite.Panier;
import java.sql.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import utils.Datasource;
import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author User
 */
public class Panierservice  implements Iservice<Panier>{
 private Connection conn;
    private Statement ste;
    private PreparedStatement pst;
  
    private ResultSet rs;
    static final String QUERY = "SELECT id, titre, quantite ,prix FROM panier";
    public Panierservice(){
           conn=Datasource.getInstance().getCnx();
    }
  
    @Override
    public void insert(Panier p){
        String req="insert into panier (titre,quantite,prix) values ('"+p.getTitre()+"','"+p.getQuantite()+"','"+p.getPrix()+"')";
        try {
            ste=conn.createStatement();
               ste.executeUpdate(req);
        } catch (SQLException ex) {
            Logger.getLogger(Panierservice.class.getName()).log(Level.SEVERE, null, ex);
        }
     
    } 
    
     public List<Panier> read() {
                    String req="select * from panier";
                    List<Panier> list=new ArrayList<>();
        try {
            ste=conn.createStatement();
            rs= ste.executeQuery(req);
            while(rs.next()){
                list.add(new Panier(rs.getInt("id"), rs.getString("titre"), rs.getInt("quantite"), rs.getInt("prix")));
            }
        } catch (SQLException ex) {
            Logger.getLogger(Panierservice.class.getName()).log(Level.SEVERE, null, ex);
        }
        return list;
    }
     @Override
    public void delete(Panier t) {
      try(
         Statement stmt = conn.createStatement();
      ) {		      
         String sql;
          sql = "DELETE FROM panier  WHERE id='" + t.getId() + "'";
         stmt.executeUpdate(sql);
         ResultSet rs = stmt.executeQuery(QUERY);
        System.out.println("personne supprimée!");
      } catch (SQLException ex) {
         Logger.getLogger(Panierservice.class.getName()).log(Level.SEVERE, null, ex);
      } 
   }
    
public void update(Panier t) {
    
        String requete = "UPDATE panier SET titre = 'xxxhh' " + " WHERE id = " + t.getId() ;
     
        try {
            
            PreparedStatement pst = conn.prepareStatement(requete);
            
            pst.executeUpdate(requete);
            System.out.println("panier modifiée!");
            
        } catch (SQLException ex) {
              Logger.getLogger(Panierservice.class.getName()).log(Level.SEVERE, null, ex);
        }
   }

    @Override
    public Panier readById(int id) {
        throw new UnsupportedOperationException("Not supported yet."); //To change body of generated methods, choose Tools | Templates.
    }
    
}
